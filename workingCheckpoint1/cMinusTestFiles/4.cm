/*A program to find out if a given year is a leap year

	
	Errors:
		declaratino and assignment at the same time
		error in if statement expressions
		
*/

void main(int x, int y)
{
	int year = a;
	
	int leap;
	int notLeap;
	leap = 2;
	notLeap = 1;

	if(year = 0)
	{
		if(year % 100 != 0)
		{
			output(leap);
		}
		else 
		{
			if(year == 0)
			{
				output(leap);
			}
		}
	}
	else
	{
		output(notLeap);
	}
}
